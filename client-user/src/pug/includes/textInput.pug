if inputData.label
	label(for=`${inputData.id}` class=`${inputData.label_hidden}`)= inputData.label
input.form-control(type=`${inputData.type}` id=`${inputData.id}` placeholder=`${inputData.placeholder}`)